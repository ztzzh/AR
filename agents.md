# AR 首饰项目代理规范

## 1. 项目定位

本项目是面向珠宝、首饰和饰品商家的 Web AR 试戴与 AI 商品内容平台。

第一阶段只聚焦一个可验证的商业闭环：

```text
用户打开手机网页
-> 授权摄像头
-> 手部识别
-> 手链或手表跟随手腕移动
-> 截图或分享
-> 点击购买
```

项目的详细产品计划以根目录 `plan.md` 为准。任何实现、拆分任务和技术取舍都必须优先服务于第一版闭环。

## 2. 当前阶段

启动基准日期：2026-09-01。

当前优先级是“里程碑一：技术原型”：

1. 创建前端项目。
2. 打开手机摄像头。
3. 接入手部关键点识别。
4. 在 Canvas 中显示关键点。
5. 使用测试 PNG 跟随手腕。
6. 完成首轮手机测试。

技术原型的验收标准：

> 一条手链可以在手机浏览器中稳定跟随手腕移动。

不要在技术原型阶段提前实现完整商家后台、支付、复杂电商 API、实时 3D 或高斯泼溅。

## 3. 推荐技术栈

### 3.1 第一阶段

- 前端：Next.js、React、TypeScript。
- 样式：Tailwind CSS，或项目已经采用的组件系统。
- 摄像头：浏览器 `MediaDevices.getUserMedia()`。
- 手部追踪：MediaPipe Hand Landmarker Web。
- 试戴渲染：Canvas 2D。
- 商品素材：透明 PNG。
- 状态管理：优先使用 React 状态；只有状态跨页面或跨模块变复杂时再引入状态库。
- 数据：原型阶段可使用本地 JSON 和静态素材。

### 3.2 MVP 阶段

- 后端：FastAPI、Python。
- API 数据校验：Pydantic。
- 数据库：PostgreSQL。
- 对象存储：S3 兼容对象存储或项目所在地区的合规对象存储。
- 异步任务：Redis 加 RQ 或 Celery，只有在 AI 图片处理等任务确实需要异步时引入。
- 测试：Vitest、Playwright。

### 3.3 后续阶段

- 3D：Three.js、GLB/GLTF。
- AI：自动抠图、透明图生成、商品分类、质量检测和锚点推荐。
- 渠道：独立链接、iframe、JavaScript SDK、微信小程序和官方电商开放平台。

## 4. 推荐目录

目标目录结构：

```text
apps/web
services/api
services/ai-worker
packages/tryon-core
packages/ui
```

职责边界：

- `apps/web`：页面、摄像头权限、用户交互、商品展示和截图。
- `packages/tryon-core`：关键点到商品变换的纯逻辑，包括定位、缩放、旋转、平滑、镜像和置信度判断。
- `packages/ui`：跨页面复用的界面组件。
- `services/api`：用户、商家、商品、试戴配置、统计和发布接口。
- `services/ai-worker`：抠图、图片质量检测、分类和其他耗时 AI 任务。

`tryon-core` 不应依赖 React、DOM 或具体页面组件，以便未来复用到独立站和小程序。

## 5. Web AR 实现规则

### 5.1 摄像头

- 摄像头必须在用户明确操作后请求权限。
- 必须提供授权前说明、授权失败提示和无摄像头降级状态。
- 优先使用前置摄像头，但保留切换摄像头的扩展能力。
- 默认不上传、不保存摄像头视频。
- 摄像头页面必须考虑 HTTPS、iOS Safari、Android Chrome 和微信内置浏览器。

### 5.2 手部追踪

- 支持左手和右手。
- 明确处理摄像头镜像坐标与实际绘制坐标。
- 使用关键点置信度阈值，低置信度时隐藏或淡出商品。
- 多手同时出现时，要有确定的选手策略，不得随机跳转。
- 手部离开画面后，商品应隐藏或平滑淡出，不能停在旧位置。
- 推理和渲染不要互相阻塞；必要时将检测放入 Web Worker。

### 5.3 手链变换

商品配置至少支持：

```text
anchorX
anchorY
scale
rotationOffset
widthRatio
opacity
zIndex
leftHandSupported
rightHandSupported
```

变换计算必须基于关键点和相对比例，不能只依赖固定屏幕像素。

第一版应实现：

- 手腕中心定位。
- 手腕方向计算。
- 商品缩放。
- 旋转偏移。
- EMA 或 One Euro 等平滑滤波。
- 商品透明度渐变。
- 截图时合成视频和商品图层。

## 6. 产品范围控制

### 第一版必须支持

- 用户无需注册即可打开试戴页面。
- 手机浏览器摄像头授权。
- 手链或手表试戴。
- 左右手识别。
- 商品切换。
- 截图。
- 分享或复制链接。
- 购买链接跳转。
- 商家上传商品并校准参数。
- 基础试戴次数和购买点击统计。

### 第一版暂不实现

- 全品类珠宝。
- 精确戒指尺寸测量。
- 复杂叠戴。
- 实时高斯泼溅。
- 原生 App。
- 自动支付和订单系统。
- 用户社区。
- 未授权的淘宝或拼多多网页抓取。

## 7. 前端体验要求

- 手机优先，竖屏优先，同时保证横屏不崩坏。
- 摄像头画面是主要内容，不要用营销型首页替代实际试戴体验。
- 主要操作必须在试戴页面内完成，减少不必要的页面跳转。
- 加载、授权、识别中、识别失败、无手部、截图成功和网络失败都必须有明确状态。
- 控件要适合单手操作，按钮点击区域不能过小。
- 商品信息、颜色切换、截图和购买操作不得遮挡手腕识别区域。
- 不要添加与珠宝试戴无关的装饰性大图、渐变背景或复杂动画。
- 文字必须适配窄屏，不能溢出、重叠或遮挡摄像头控制项。
- 使用已有图标库时优先使用库内图标，不手绘重复图标。

## 8. 商家商品素材规则

- 原图和透明底图分开保存。
- 上传后检查尺寸、格式、透明通道和文件大小。
- 商品图必须允许商家预览和重新校准。
- AI 处理结果只能作为建议，必须允许人工确认和调整。
- AI 不能作为真实珠宝结构的唯一来源。
- 记录素材来源、授权状态和处理模型。

推荐素材授权字段：

```text
asset_source
license_name
commercial_allowed
attribution_required
restriction_note
review_status
```

## 9. 数据与隐私

- 摄像头使用前必须说明用途并取得用户授权。
- 默认不保存摄像头视频。
- 用户上传照片要有保存期限和删除机制。
- API 不得在日志中记录原始摄像头帧、敏感用户照片或访问令牌。
- 商品素材、模型和 AI 生成内容必须有授权状态。
- 隐私政策、用户协议和数据删除流程属于发布前验收项。
- 国内和海外版本应分别检查适用的隐私、数据和知识产权要求。

## 10. 测试要求

每个影响试戴定位的改动都至少验证：

- iPhone Safari。
- Android Chrome。
- 明亮、昏暗和逆光。
- 左手和右手。
- 手腕靠近和远离摄像头。
- 手掌旋转。
- 手部暂时离开画面。
- 多人同时出现在画面。
- 网络较慢。

重点观察：

- 摄像头授权成功率。
- 首屏和模型加载速度。
- 关键点识别稳定性。
- 商品位置抖动。
- 平均帧率。
- 识别失败后的恢复行为。
- 截图是否包含完整视频和商品图层。

涉及布局、摄像头或 Canvas 的修改，应使用 Playwright 或真实手机进行验证；不能只依赖桌面浏览器静态检查。

## 11. 开发工作流

开始任务前：

1. 先阅读根目录 `plan.md` 和本文件。
2. 检查现有代码、依赖、脚本和未提交修改。
3. 明确任务属于原型、MVP、商家后台、AI 处理还是后续扩展。
4. 选择最小改动范围，避免提前搭建无用基础设施。

实现过程中：

1. 遵循现有项目结构和命名方式。
2. 将 AR 数学逻辑与页面交互分离。
3. 为关键状态提供可见反馈。
4. 不覆盖或回滚用户已有的未提交修改。
5. 不为了“未来可能需要”提前引入大型依赖。

完成前：

1. 运行与改动相关的 lint、类型检查和测试。
2. 对摄像头、Canvas 和移动布局进行实际验证。
3. 检查是否引入隐私、授权或素材泄露风险。
4. 更新必要的文档和验收状态。
5. 在结果中说明改动、验证方式和剩余风险。

## 12. 完成定义

一个任务只有在以下条件都满足时才算完成：

- 功能在目标流程中可操作。
- 失败状态和恢复路径已经处理。
- 不破坏桌面和移动端基本布局。
- 相关测试或人工验证已经执行。
- 没有绕过授权、隐私或素材许可要求。
- 没有把后续阶段功能混入当前阶段，造成不必要的复杂度。

## 13. 技术取舍原则

遇到多个方案时，按以下顺序决策：

1. 能否帮助验证“打开、试戴、截图、购买”闭环。
2. 是否适合手机浏览器和低端设备。
3. 是否能让商家用普通商品图开始配置。
4. 是否保留未来升级到 3D、AI、独立站和小程序的空间。
5. 是否降低部署、调试和维护成本。

第一版成功的标准不是技术栈完整，而是用户能够稳定试戴并愿意点击购买。
